import React from 'react';
import ReactDOM from 'react-dom';

// import app that doesn't exist yet
import App from './App';

// mount app onto root for every react app
ReactDOM.render(<App />, document.getElementById('root'));

